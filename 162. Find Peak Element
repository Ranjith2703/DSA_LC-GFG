class Solution {
    public int findPeakElement(int[] nums) {
        int in=0;
        long m=Long.MIN_VALUE;
        for(int i=0;i<nums.length;i++){
            if(nums[i]>m){
                m=nums[i];
                in=i;
            }
        }
        return in;
        
    }
}
