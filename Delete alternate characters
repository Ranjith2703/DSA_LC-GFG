class Solution {
    static String delAlternate(String S) {
        StringBuilder a=new StringBuilder();
        for(int i=0;i<S.length();i+=2){
            a.append(S.charAt(i));
        }
        return a.toString();
    }
}
